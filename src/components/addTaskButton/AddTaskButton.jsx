import React, { Component } from 'react'
import { AddButton } from './addTaskButtonStyle'

export default class AddTaskButton extends Component {
  render() {
    return <AddButton> Adicionar nova tarefa</AddButton> 
  }
}
